
"use strict";

var event = require('./Events');

class Ticket {
    constructor(ticket_holder, seat_no, event_ID)
    {
        this.ticket_holder = ticket_holder;
        this.seat_no = seat_no;
        //Look up event ID to retirve price, date, event title.

    }

    //static ticket ID
}

module.exports = Ticket;
